use std::time::{Duration, Instant};

pub struct Scheduler {

}

impl Scheduler {
  pub fn new () -> Self{
    Self {}
  }

  fn create_task() {
    let when = Instant::now() + Duration::from_millis(100);
  }

  fn update_interval() {

  }
}
